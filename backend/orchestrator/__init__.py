"""Orchestrator package — LangGraph state machine for agent coordination."""
# Note: avoid circular imports by NOT importing graph at package level.
# Import orchestrator.graph directly in main.py and tests.
